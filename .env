# Vite 开发服务器端口（改端口时请同步修改 src-tauri/tauri.conf.json 的 build.devUrl）
VITE_DEV_PORT=1420
# 后端 API 端口，langchain-serve 监听此端口，前端 axios 通过代理或 baseURL 访问
VITE_API_PORT=8264
# PTY WebSocket 服务端口，pty-host 监听此端口，前端 ws 连接用
VITE_PTY_PORT=8265
